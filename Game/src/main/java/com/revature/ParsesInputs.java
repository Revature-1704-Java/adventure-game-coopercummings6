package com.revature;

interface ParsesInputs
{
    //This method returns a string which contains "North", "South". "East", or "West".
    //it gets the values by prompting the user for input, and will validate the user's input.
    public String getCommand();
}